all:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: /mnt/docker/ssh.key
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
    ansible_python_interpreter: /usr/bin/python3

  children:
    my_one_host:
      hosts:
        rc: